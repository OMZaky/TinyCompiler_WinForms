# 🛠️ TINY Language Compiler (C#)

[![Language](https://img.shields.io/badge/Language-C%23-239120.svg)](#)
[![Phase](https://img.shields.io/badge/Phase-Lexical_Analysis-blue.svg)](#)
[![Institution](https://img.shields.io/badge/Institution-Ain_Shams_University-goldenrod.svg)](#)

A robust, multi-phase compiler implementation for the extended TINY programming language, engineered entirely in C#. This project is developed as part of the Semester 4 Compiler Construction coursework.

## 📖 About the TINY Language
TINY is a pedagogical programming language designed for learning compiler architecture. This specific implementation supports an extended 30-rule grammar, featuring:
* **Data Types:** `int`, `float`, `string`
* **Control Structures:** `if-elseif-else-end`, `repeat-until`
* **Functions:** Full support for function declarations, parameters, and a required `main()` entry point.
* **Operators:** Standard arithmetic, assignment (`:=`), conditional (`<>`, `<=`, etc.), and boolean (`&&`, `||`) operators.
* **Comments:** Multi-line block comments (`/* ... */`).

---

## 🏗️ Compiler Architecture Pipeline

The compiler is being built in distinct phases, translating raw source code through a standard processing pipeline.

### Phase 1: Lexical Analysis (Scanner) — ✅ *Completed*
The Scanner acts as the front-end processor. It reads the raw TINY source code character by character and groups them into meaningful, categorized `Tokens`.
* **Engine:** Custom-built deterministic state machine using pre-compiled Regular Expressions (.NET flavor).
* **Features:**
  * **O(1) Keyword Validation:** Efficient separation of generic identifiers from reserved keywords.
  * **Precision Tracking:** Accurate line numbering that dynamically handles multi-line comments and whitespace blocks.
  * **Robust Error Handling:** Generates `Unknown` tokens for illegal characters, preventing compiler crashes and infinite loops.
  * **Parser Safety:** Injects an `EOF` (End of File) token to secure the syntax tree boundaries.

### Phase 2: Syntax Analysis (Parser) — 🚧 *In Progress*
The Parser will consume the token stream generated by the Scanner and verify it against the TINY Context-Free Grammar (CFG).
* Generates an Abstract Syntax Tree (AST).
* Catches and reports structural syntax errors (e.g., *Syntax Error: Missing ';' on Line 15*).

### Phase 3: Semantic Analysis & Code Generation — 📅 *Upcoming*
* **Symbol Table:** Type checking and scope validation for functions and variables.
* **Target Code:** Translating the validated AST into intermediate representation or target machine code.
---

## Project Structure

```text
📁 TinyCompiler
 ┣ 📄 TinyCompiler.csproj   # C# Project configuration file
 ┣ 📄 Program.cs            # Main execution entry point containing sample TINY code
 ┣ 📄 Scanner.cs            # The core lexical analyzer engine and RegEx dictionaries
 ┣ 📄 Token.cs              # Data models for Tokens, TokenTypes, and formatted console output
 ┗ 📄 README.md             # Project documentation and architecture overview
```
---

## 🚀 Getting Started

### Prerequisites
* [.NET SDK](https://dotnet.microsoft.com/download) (Version 6.0 or higher recommended)
* An IDE like Visual Studio Code or Visual Studio.

### Installation & Execution
1. Clone the repository:
   ```bash
   git clone [https://github.com/OMZaky/TinyCompiler.git](https://github.com/OMZaky/TinyCompiler.git)
   cd TinyCompiler

2. Run the compiler:
   ```bash
   dotnet run
